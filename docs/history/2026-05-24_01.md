# 2026-05-24 (深夜セッション) — CLAUDE.md 第二弾リファクタ

## サマリ

CLAUDE.md の改善余地を ultrathink で調査 → 10 項目を全て実施。
578 → 506 行 (▼72 行、▼12.5%)。構造的重複排除と §13 のドメイン規約外出しが主眼。

## ブランチ・コミット

- `feature/claude-md-slim`、`feature/gemini-cost-stable-loops` から **4 コミット先行**
- 本セッションの新規コミット: `3e90e7b`
- push 済み (本セッション末)

## 実施した 10 項目

| # | 項目 | 結果 |
|---|---|---|
| 1 | Opus 4.6 → 4.7 (1M context) | §2 コミットテンプレ更新 |
| 2 | HSV 関数移行リスト整理 | 原則のみ残し、残存箇所は `git grep` で確認に変更 |
| 3 | §12/§14 起動コマンド表重複 | §14 に一本化、§12 はリンクのみ |
| 4 | §11/§17/§21 Gemini 重複 | §22 に「共通実装ルール」として集約 |
| 5 | §13 メタ vs 実装規約分離 | 協働メタ 9 ルールに絞り、実装規約 11 個を `docs/scene_detection_rules.md` (83 行) へ外出し |
| 6 | §0 削除 | §12 に 1 行ポインタ、本体は `docs/tutorial_autopilot.md` |
| 7 | 「厳格・最重要」整理 | §11 §13 の真の 2 箇所のみ、§21 §22 は「厳格」に降格 |
| 8 | 「未対応 (次回タスクで揃える)」削除 | STATUS.md 中優先タスクに一本化 |
| 9 | §0/§6 番号欠番注記 | 冒頭に注記追加 |
| 10 | §15 配置 | リンク互換性のため移動せず、§13 冒頭の補足で関連を明示 |

## §13 構造の変化

**Before** (15 サブセクション混在):
- 協働メタと実装規約が混ざり、「最重要」ルールが中盤に埋もれていた

**After** (9 サブセクション、協働メタのみ):
1. CLAUDE.md 参照義務 ← 冒頭に昇格 (毎ターンの起点)
2. コード修正前のユーザー確認（厳格・最重要）
3. --fresh-install (`-r`) は指示直後の1回のみ
4. Git ワークフロー（厳格）
5. ゲーム仕様の憶測禁止
6. 「理由を教えて」は説明のみ
7. ルールの永続化
8. セッション締めに高速化調査
9. 用語統一: 「間引き」ではなく「クラスタリング」

実装規約 11 個 → `docs/scene_detection_rules.md` (該当コード編集時のみ参照):
HSV 検出禁止 / 動画タップ禁止 / バトル初回ダイアログ棄却 / 指+金枠タップ /
detect_scene_early 契約 / 解析基準解像度 / PilotState/CycleState 分離 /
OCR エンジン互換 / 画像認識ドキュメント更新 / シーン検出器修正時テスト

## §22 の集約

§17 §21 にあった Gemini 共通実装ルールを §22 に統合:
- 送信方式: `Part.from_bytes` インライン
- 並列化: ThreadPoolExecutor 5 並列
- エラー時キャッシュ禁止
- キャッシュキーに `model` を含める

§17 §21 には固有制約のみ残す:
- §17: lc_anchor_judgments テーブル、「迷ったら true」
- §21: (master_fp, tag_type, prompt_hash, model) キー、scene カラム別物

## ユーザー対話

1. ユーザー: 「CLAUDE.md のさらなる改善点がないか調査して ultrathink」
2. Claude: 10 項目を 🔴 / 🟡 / 🟢 で分類して提示
3. ユーザー: 「1からすべて対応して　こちらへの確認は不要」 ← 明示的な承認
4. Claude: 新規 docs + CLAUDE.md 全面書き換え + STATUS.md 更新 + コミット
5. ユーザー: 「pushして　その後セッション終了する」

## 累積成果 (本ブランチ)

| 段階 | CLAUDE.md 行数 | 削減率 |
|---|---|---|
| リファクタ前 | 985 | — |
| 第一弾後 (前回セッション) | 578 | ▼41.3% |
| 第二弾後 (本セッション) | **506** | ▼48.6% |

新規 docs ファイル (本ブランチで作成された外出し先):
- `tutorial_autopilot.md` (101 行)
- `evidence_recording.md` (24 行)
- `cleanup_procedure.md` (要約済み)
- `gemini_prompt_design.md` (要約済み)
- `scene_detection_rules.md` (83 行、本セッションで新規)

## テスト

- `pytest crawler/tests/test_gemini_prompt_cache.py`: 13/13 pass

## 次セッションの引き継ぎ

`feature/claude-md-slim` ブランチは origin にも反映済み。
このブランチを PR にするか、`feature/gemini-cost-stable-loops` と一緒にまとめるかはユーザーの判断待ち (§13 Git ワークフロー: PR は明示指示時のみ)。

未着手の中優先タスクは STATUS.md 参照:
- anchor_matcher.py (P4-P6) の SYSTEM/USER 分離
- tag_judgment.py の SYSTEM/USER 分離
- `cachedContentTokenCount` ログ追加
- 実機検証で案 A + C の効果測定 (期待: 月 $25 → $5-8)

🔴 必須対応 (継続課題):
- BG worker 漏れ修正 (5 メソッド、feature/tagging)
- ConfirmDialog のスキップ確認区別 (ムービー vs ストーリー)
