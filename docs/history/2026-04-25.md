# セッション 2026-04-25（深夜）

## クラスタリング§16違反修正 (前日からの継続)
- ステップ1のグローバル検索→直前クラスタのみ比較
- バリデーション閾値12→30統一+分離後の時系列再グループ化
- Gemini remerge の総当たり→直前クラスタのみ比較

## Gemini OCR 1枚並列化 + REST API 化
- バッチ4枚→1枚1リクエスト×8並列（コンテキスト汚染防止）
- google-genai SDK 1.73.1の画像送信バグ発見→urllib REST API直接呼び出しに変更
- APIエラー時はNULL維持（次回再送信）
- Gemini結果の非代表への伝播→廃止（remergeとの相互作用で誤統合発生）

## Python 3.11 + OpenSSL 3.6.2 移行
- pyenvでPython 3.11.8をビルド（openssl@3連携）
- venv再作成、requirements.txtにpaddlepaddle,ocrmac追加

## ImageComparator 導入（全Phase完了）
- lc/image_comparator.py: Strategy パターンで phash/dHash を切替
- 環境変数 LC_HASH_ALGO で制御（デフォルト: dhash）
- Phase 1-3: コア+クラスタリング+バリデーション+remerge
- Phase 4: screen_recorder の startup phash比較
- Phase 5: anchor_matcher の全6 Phase
- Phase 6: handlers, image_proc, batch_processor, cross_session_merger
- DB に phash/dhash 両カラム保存、即座に切替可能

## UI修正
- クラスタ兄弟APIにocr_text_gemini追加（テキスト表示空問題）
- Gemini補正テキストが数字のみの場合のタイトル未更新修正
- Live代表切替後の一覧サムネ即時反映
- _SKIP_SCENESにSTARTUP追加

## コミット数
約20コミット
