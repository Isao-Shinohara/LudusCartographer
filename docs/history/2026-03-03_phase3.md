# Phase 3 セッション要約 — 2026-03-03

## 実施内容

Phase 2-3 の DFS クローラーに以下を追加した。

### A. lc/crawler.py — 修正
1. **`_extract_title` 改善 (2ステップ方式)**
   - EXCLUDE_TEXTS: `戻る` / `Done` / `完了` / `Edit` / `編集` / `キャンセル` を除外
   - Step1: Large Title (x < 400, y = 150-750) — 左寄りの大タイトル優先
   - Step2: Nav-bar center title (x = 300-900, y = 100-260) — フォールバック
   - 「戻る」が y=204 に出ていた問題を解消

2. **phash 重複判定 (`PHASH_THRESHOLD = 8`)**
   - `ScreenRecord.phash` フィールド追加
   - `_snapshot_current_screen` で `compute_phash()` を実行
   - `_crawl_impl` でテキスト指紋チェック前に phash ハミング距離 < 8 → `[PHASH_DUP]` ログ + skip

3. **`save_summary_json(path)` 追加**
   - visited screens の fingerprint/title/depth/parent_fp/tappable_items/phash を JSON 出力
   - `_finalize_session()` 内で `evidence/<session_id>/crawl_summary.json` に自動保存

### B. lc/utils.py — 追加
- `compute_phash(image_path, hash_size=8)` → 16 文字 hex (DCT phash、cv2.dct 使用)
- `phash_distance(h1, h2)` → ハミング距離 (0〜64)

### C. tools/__init__.py — 新規 (空ファイル)

### D. tools/visualize_map.py — 新規作成
- `load_summary(session_dir)` — crawl_summary.json 読み込み
- `load_from_db(...)` — MySQL screens テーブルから読み込み
- `build_graph(screens)` — dict ベースの遷移グラフ {nodes, edges}
- `render_mermaid(graph)` — Mermaid `graph TD` 出力 (unknown ノードは fill:#ffcccc)
- `render_tree(graph, root_fp)` — ASCII ツリー (📱/├──/└── スタイル)
- `analyze_gaps(screens)` — unknown / 疑惑タイトル / タップ候補なし レポート
- `format_gaps(gaps)` — 人間が読める形式にフォーマット
- `main()` — CLI (--session / --db / --host / --format mermaid|tree|gaps|all)

### E. tests/test_visualize_map.py — 新規作成 (10 テスト)
- test_build_graph, test_render_mermaid, test_render_tree, test_analyze_gaps
- test_compute_phash_basic, test_phash_distance_same, test_phash_distance_different
- test_load_summary, test_load_summary_missing
- test_visualize_integration (crawl_summary.json があれば実行、なければ skip)

## テスト結果

```
110 passed, 4 skipped
```

## コミット

`b626017` feat(phase3): 遷移マップ可視化・phash重複判定・タイトル抽出改善
