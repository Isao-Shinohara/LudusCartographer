# 2026-04-21 セッション要約

## 概要
6段階アンカーマッチング（P4テキストGemini新設・P5/P6画像判定分離）の実装、Phase間データフローの厳格化、キャッシュバグ修正、UI大幅改善。25コミット。

## 主な作業

### 1. CLAUDE.md ルール永続化 (§17-19)
- §17 アンカーマッチング設計ルール: 6段階Phase定義、テキスト類似度計算、ノイズ除去
- §18 バージョン管理ルール: lc_versions スキーマ、version_id 必須テーブル
- §19 SafeInsert 安全挿入方式: 原則と挿入可能条件

### 2. 6段階アンカーマッチング (P4テキストGemini新設)
- 旧P4(画像flash-lite)→P5、旧P5(画像flash)→P6 に振り直し
- 新P4: テキストのみGemini送信（画像なし、トークン1/10〜1/20）
- `_phase4_gemini_text` + `_gemini_text_judge` メソッド新設
- PHASE_DEFS 6段階化、DB既存データのmethod名マイグレーション
- `prompt_template` の `{is_same}` が `str.format()` と衝突するバグを修正（`{{}}` エスケープ）

### 3. Phase間データフロー厳格化
- P4棄却→P5画像再検証フロー実装
- P5検証対象: P3確定のみ（P1/P2/P4確定は検証不要）
- P3アンカーがP5検証通過時にmethod/phaseをP5に更新
- P5でP4棄却と新規候補のsession_fp重複排除

### 4. キャッシュ修正
- エラー時キャッシュ禁止ルール（§17厳格ルール）: `error=True` フラグ付与、キャッシュスキップ
- P5キャッシュ検索に `AND model = 'gemini-2.5-flash-lite'` 追加（P4キャッシュ誤ヒット防止）
- P4キャッシュで他model(P5/P6)の確定済み判定も参照（クロスモデルキャッシュ）
- `lc_anchor_judgments` に `model` カラム追加

### 5. テスト追加 (14→25件)
- P4: 確定/棄却タプル返却、エラーキャッシュ禁止、キャッシュヒット、クロスモデルキャッシュ
- P5: P4棄却受け渡し、P3検証、P3→P5更新、P1/P2スキップ、modelフィルター、エラーキャッシュ禁止
- P6: P5棄却復活

### 6. UI改善
- Cost タブ: USD→JPY円表示（open.er-api.comリアルタイム為替、取得失敗時はドル表示）
- Phase バッジ: トグルボタン化（初期全ON、タップでOFF、複数フィルタ対応）
- Phase 説明パネル: (i)ボタンでP1〜P6の処理内容・棄却フロー・キャッシュ説明を表示
- モーダルページング: Phaseフィルター反映（非表示カードをスキップ）
- 新規追加ノード・アンカー一覧に全ノード数表示（例: 5/709, 採用529/709）

### 7. プレビュー安定化
- `_preview_merge.py` に try-except + traceback 出力追加
- `merge_progress` のJSON読み取り失敗時にファイルを削除しないよう修正
- フロントのポーリングにエラーログ追加

## パフォーマンス
- 100%キャッシュ時: **5.0秒** で529アンカー / 709ノード
- 初回（API送信あり）: 約8-10秒（P4テキスト+P5/P6キャッシュヒット時）
- P4テキスト判定: 233件中221件確定、12件棄却→P5画像再検証

## 未解決・次回作業
- Gemini モデルを Settings から設定変更可能に
- prefer フィールドの活用（マスター OCR テキスト更新）
- 周回3以降でアンカー数の増加検証
- プレビュー画面遷移の安定性（ポーリングタイミング問題）
