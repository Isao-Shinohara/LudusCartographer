# セッション: 2026-04-19 22:00〜

## 概要
マージプレビューの高速化、Phase 2 Gemini アンカーマッチング実装、ダッシュボードUI大幅改善、バージョン管理機能実装。約50コミット。

## 主な変更

### マージプレビュー高速化
- `_get_neighbors()` を個別SQLクエリ(699回)からバッチ一括取得に変更: 396秒→0.37秒 (1,070倍高速化)
- `lc_screens`, `lc_transitions` に複合インデックス追加

### Phase 2 Gemini アンカーマッチング (重要)
- phash < 5 + SequenceMatcher >= 0.7 の候補を Gemini Flash に同一画面判定依頼
- `lc_anchor_judgments` テーブルでキャッシュ (テキストハッシュベース)
- アンカー数が 9件 → 55件に大幅増加 (セッション ap_20260418_153601)
- Phase 番号を P1/P2/P3/P4 に振り直し
- プレビューでGバッジ表示、ユーザーによる除外機能

### ダッシュボードUI改善
- カード表示を `buildCard()` 共通関数に統一 (Live/Final/プレビュー)
- マージプレビュー: 新規追加ノード (SafeInsert判定済み) + アンカー一覧
- モーダル: ヘッダー全幅、詳細+同クラスタ横並び統合、↑↓でクラスタ選択
- Final デバッグ: セッション選択セレクトボックス追加、フッターにセッション情報
- 日時フォーマット: YYYY/MM/DD HH:MM:SS に統一
- 未完走バッジ色を暗い水色に変更
- マージ完了セッションを新しい順に表示

### グラフ構築改善
- 代表画面のみでグラフ構築 (不採用画面のノイズ除去)
- phash Union-Find を削除し、クラスタリング済みの代表fpにリダイレクト

### バージョン管理機能 (大規模)
- `lc_versions` テーブル新設 (id, name, created_at, is_active, is_deleted)
- `lc_sessions`, `lc_master_nodes`, `lc_master_edges`, `lc_node_mappings`, `lc_session_graphs` に `version_id` カラム追加
- Python: ScreenRecorder, BatchProcessor, CrossSessionMerger (38クエリ), AnchorMatcher, MergeSortStrategy, BackgroundWorker 全て version_id 対応
- PHP: EvidenceRepository 全メソッド + search.php API に version_id フィルタ追加
- UI: Settings タブ (バージョン作成/リネーム/有効化/論理削除)、Final タブにバージョンセレクター
- バージョン切替時に running セッションを自動完了
- 論理削除時にローカルファイルも削除

### 仕様変更
- SafeInsert でスキップされたノードは「破棄」に明確化 (次の周回に委ねない)
- merge_sort_algorithm.md 更新

## 未対応・次セッションの課題
- バージョン管理のUI動作確認・テスト
- Phase 2 Gemini の実運用テスト (マージ実行→Finalで確認)
- Merge タブのバージョン対応 (version_id パラメータ伝搬の確認)
- STATUS.md 更新
