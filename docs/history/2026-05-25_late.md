# 2026-05-25 (深夜延長) — CLAUDE.md 第三弾リファクタ (全 Tier 改善)

## セッション概要

ユーザー指示「claud.md の改善案を出して スキルやエージェントなども考慮して ultrathink」から開始。
Plan 段階で 4 Tier (T1〜T4) + サブ案を提示、ユーザーが **全 Tier 適用** を選択 → 10 Step を順次実装。

## 入力 → 出力

- **入力 CLAUDE.md**: 506 行 (第二弾リファクタ後)
- **出力 CLAUDE.md**: 554 行 (§23 新設 40 行 + §5 責任分界 6 行 + 圧縮 -8 行 ≒ +48)
- **新規 settings.json フック**: SessionStart 拡充 + PreToolUse 危険操作ブロック
- **新規 skill**: `/review-gemini-prompt` (§22 編集時チェック 4 項目自動化)
- **docs/ 再編**: 23 → 17 + 6 archive (`docs/_archive/`)、新規 `docs/README.md` インデックス

## コミット履歴 (`feature/claude-md-slim` に 7 件追加)

| Commit | 内容 |
|--------|------|
| `743f272` | docs(CLAUDE.md): §10 ハードコード行番号 (:278) を削除 |
| `55bc499` | docs(CLAUDE.md): -r フラグ判断を §14 起動コマンド対応表に集約 |
| `93b1715` | docs(CLAUDE.md): Gemini 共通実装ルールへの参照を §17/§21 で統一表記化 |
| `551227f` | docs(CLAUDE.md): §23 エージェント/スキル活用ルールを追加 |
| `6dc5178` | docs(CLAUDE.md): §15-5 矩形テンプレマッチ詳細を scene_detection_rules.md へ外出し |
| `63666d4` | chore(docs): 孤児ドキュメント 6 件を _archive/ へ整理、現役 5 件を README.md でインデックス化 |
| `4ab044a` | docs(CLAUDE.md): §5 に CLAUDE.md/STATUS.md/docs/ の責任分界を明文化 |

settings.json / skill は `.claude/` (gitignored) のためコミット対象外、ローカル適用済み。

## 重要な意思決定

### 1. §0/§6 廃止 → SessionStart フックのバグ修正

`.claude/settings.json` の SessionStart hook が **既に廃止済みの §0 を読もうとしていた**。
CLAUDE.md 冒頭の番号欠番宣言と矛盾。§11/§13/§15 への参照に修正。
**教訓**: 番号削除時はフック・スクリプト側の参照も grep する。

### 2. `-r` フラグの Single Source of Truth 化

14 箇所に分散していた `-r` 言及を §14「起動コマンド対応表」に集約宣言。
§11 (禁止) / §13 (即時ルール) の再掲は維持、§12 共通ルールから 1 行ポインタ化。
**結果**: 14 → 13 箇所 (行数より構造的明示性が改善)。

### 3. PreToolUse フックの偽陽性回避設計

当初 `-r` 単独検出は `grep -r` / `tar -rf` を誤ブロックするため不採用。
最終的に `(auto_pilot|run_autopilot)[^|;]*[[:space:]]-r([[:space:]]|$)` と限定して
auto_pilot 文脈の `-r` のみ検出。8 ケース手動テストで誤検出ゼロ確認。

### 4. docs/ 孤児判別の事前調査

13 ファイルを `head -3 + 最終更新日 + 行数` で一括スキャンしてユーザーに暫定判別を提示。
ユーザー一発承認で 6 ファイルを `_archive/`、5 ファイルを `docs/README.md` 化。
**教訓**: 大量ファイル判別は事前に構造化して 1 質問に集約する。

## §23 で明文化したエージェント/スキル基準

| 状況 | 使用 |
|------|------|
| 3 クエリ以上の広域探索 | **Explore agent** |
| 非自明な実装方針設計 | **Plan agent** |
| 1〜2 ファイル完結探索 | 直接 grep / Read |
| 「PR レビュー」要望 | `/review` |
| 「セキュリティ観点で見て」 | `/security-review` |
| settings.json 編集 | `update-config` skill |
| 「もっとシンプルに」 | `simplify` skill |
| Gemini プロンプト編集後 | `/review-gemini-prompt` (新規作成) |

## 残課題

- `feature/claude-md-slim` のブランチを **push** するか (ユーザー指示待ち)
- 本ブランチを **PR にする** か (ユーザー指示待ち)
- `feature/gemini-cost-stable-loops` も依然未 push (前セッションから繰越)
- `feature/tagging` (28 コミット先行) も未マージ

## 次セッションでの効果検証ポイント

- SessionStart フックが `git status` + `STATUS.md` 自動確認を報告するか
- PreToolUse フックが日常作業 (grep, ls, git 等) を誤ブロックしないか
- §23 表に従ったエージェント/スキル選択が定着するか
- `docs/README.md` が新ファイル追加時のインデックスとして機能するか

## 高速化調査 (§13 セッション締めルール)

今回はドキュメント/フック編集中心のため、コードのボトルネック計測対象なし。
強いて挙げれば:
- `head -3 + wc -l + stat` の 11 ファイル走査が Bash for loop で逐次。`find -exec` 並列化で 1〜2 秒短縮可だが、合計 10 秒未満の処理のため不要
- Plan 作成時の Explore agent 2 並列起動は適切 (8 並列等は agent 起動コストで損)
