# 2026-04-30 セッション要約

## 概要

スクリーン記録機能の最終仕上げセッション。`process_session_bg` の堅牢化、`paused` 状態の導入、マージプレビューの不具合修正・機能追加を中心に **12 コミット**。テスト 102 件全 PASS。

## 主な作業

### 1. process_session_bg 系の堅牢化 (3 コミット)

#### `de7f282` 二重起動・無限リトライ・PHP exec ハング解消

ユーザー報告「process_session_bg を実行すると 30 秒タイムアウト + 同じ Gemini エラーが延々と出続ける」の根本対策。

- ❶ **二重起動防止**: `auto_pilot_state` テーブルでロック (PID + age + posix_kill aliveness check)、二度押しは `already_running` で拒否
- ❷ **JSON parse 失敗時の即時リトライ**: `gemini_correct_single` で truncated レスポンスに最大 2 回リトライ (合計 3 試行)
- ❹ **N 回連続無進展で sentinel**: 3 iteration 連続で `g` が減らなければ残 NULL に空文字 sentinel を入れて打ち切り (永遠ループ防止)
- ❺ **PHP exec → popen/pclose**: `&` バックグラウンドでも shell stdout EOF を待つ仕様で 30 秒ハングしていた問題を解消

E2E 検証: 起動 0.062s で即 return (旧 30s)、iterations=4 で正常終了、ロック解放確認。

#### `20cf431` sentinel 短縮 + 進捗キー不一致バグ修正

- `NO_PROGRESS_LIMIT: 3 → 2` (待機時間 ~33% 短縮)
- phase 細分化: `OCR / synthesizing_edges / building_graph / graph`
- **既存バグ修正**: `_update_progress` が `merge_progress` キーに書いていたが PHP 側は `merge_phase` キーを読んでいたため進捗が一切表示されない不整合を統一
- ダッシュボード JS: 単一ポーリングに統合、180s で「Gemini API 遅延の可能性」警告

#### `e12febe` 削除済みセッションを Live タブから除外

進行中セッション削除後に Live タブが削除済みを「最新」として表示し続ける問題。`getSessions` の WHERE に `status NOT IN ('archived', 'discarded')` を追加。

### 2. paused 状態の導入 (1 コミット)

#### `5bdb478` paused ライフサイクル + 確定フロー

「手動停止 (Ctrl+C) で `completed` になり、知らないうちにマージタブに並んで誤マージするリスク」をユーザーが指摘。

```
running ─SIGTERM/Ctrl+C─→ paused (manual_stop)
running ─goal_reached───→ completed (goal_reached)
running ─crash──────────→ completed (orphaned)
paused ─「完了として確定」→ completed (manual_stop)
paused ─auto_pilot 起動──→ running
```

- `screen_recorder.close(goal_reached=False)` → `status='paused'` (was 'completed')
- `getRunningSessions` が running + paused 両方を返す
- 進行中タブの paused 行に「完了として確定」ボタンを表示
- 確定時は `pgrep -f auto_pilot.py` で生存チェック → 動いていたら拒否
- 既存スキーマバグ修正 (`is_representative` カラムが CREATE TABLE に欠落) も副次対応 → 23 件のテスト失敗を解消

### 3. マージプレビューの修正・機能追加 (5 コミット)

#### `518071a` seed プレビューで新規追加 0 件誤表示

マスター空 (= seed) のとき `preview_merge` が「新規追加ノード 0 件」と誤表示する不整合 (実 merge では `_seed_master` が全代表を INSERT するのに、プレビューは SafeInsert を呼んで「アンカーがないので挿入不可」と判定していた)。

修正: `is_seed=True` の場合 SafeInsert を呼ばず全 new_fps を `insertable=True` で返す。

実機検証: ap_20260428_122410 (1070 reps) で `insertable: 0/1070 → 1070/1070` ✓

#### `42b93ea` Live で不採用にした画像が merge に混入

`_compute_matches` の `session_reps` SELECT が `is_artifact` フィルタを欠いていた整合性違反 (実 merge_to_master と AnchorMatcher は除外しているのに preview だけ未除外)。

`AND COALESCE(is_artifact, 0) = 0` を追加。

#### `59d95c1` マスター OCR 手動編集時のキャッシュ自動削除

P4-P6 Gemini 判定キャッシュ (`lc_anchor_judgments`) は `(session_fp, master_fp, model)` キーだが、`master_fp` は画面 fingerprint なので OCR 手動編集しても変わらず、編集前テキストで下した古い判定が永続的に効いてしまう問題。

`update_manual_text` / `apply_correction_rule` の UPDATE 直後に該当 `master_fp` のキャッシュを `DELETE`。確認は出さず自動。

副次修正: `update_manual_text` の SELECT が `lc_master_nodes` 直接で `ocr_text_gemini` を参照していた既存バグ (`lc_screens` 側のカラム) → JOIN で取得するよう修正。

#### `62b4753` マージプレビューで新規追加ノードに「不採用」操作

ユーザー要望「Mergeのプレビューで新規追加ノードに対しても不採用選択ができるようにしてほしい」。

`is_artifact=2` (Live と同じ) を流用することで、既存の「`session_reps` から `is_artifact > 0` を除外」ロジック (`42b93ea` で追加) が自動的に効く設計。

- backend: `new_nodes` に `screen_id` 追加
- frontend: モーダルに「✕ 不採用にする (この新規ノードを除外)」ボタン
- クリック → `toggle_screen_artifact` API → プレビュー再取得

E2E: ap_20260428_122410 で screen 128507 を不採用 → new_nodes 375→374、対象消失 ✓

#### `5f8c864` shift+クリック選択 → 一括不採用に新規ノード対応

shift+クリックの対象を「アンカーのみ」→「アンカー + 新規ノード」に拡張。確定ボタンは選択を anchors / newNodes に分岐:

| 種別 | 状態保持 | UI | 取り消し |
|---|---|---|---|
| アンカー (master_fp あり) | excludedGeminiFps Set | 半透明オーバーレイ | shift+選択 + 採用 |
| 新規ノード (screen_id あり) | DB.is_artifact=2 | プレビューから消失 | Live タブから |

#### `7aa52a2` マージ実行で空ページ問題

ユーザー報告「マージ実行後にマージ待ちが 0 件、更新したら 5 件出てくる」(2 ページ目を見ていたのに件数が 1 ページに収まる量に減った場合に発生)。

`_renderMergeSection` の冒頭で page index を `totalPages-1` にクランプ + writeback。

### 4. ドキュメント整理 (2 コミット)

#### `5683c94` 2026-04-24〜26 の欠落セッション要約

3 日分のセッション要約 (4/24-25, 4/25, 4/26) が docs/history/ に未コミットのまま放置されていた。実装コードは既に対応する 89 件のコミットで反映済み。

ただし 4/26 の `lc/scene_boundary_detector.py` (ヒスト + バタチャリヤ) は翌日 `b314150` のリファクタリングで phash + dHash 二段構えに置き換えられたため、現コードには存在しない。当時の設計判断と転換の経緯を記録として残す。

#### `6fcf19c` ocr_learned_patterns.json コミット

CLAUDE.md §14 で「保護対象 (絶対に削除しない)」とされる学習資産。confirmed 16 件 / candidates 303 件。

## ユーザーとの主要対話

### Q. 「P4-P6 は元々自動操縦時に実行していなかった？いつからマージ時になった？」

A. 元から CrossSessionMerger 経由 (= マージ時) のみ。`9dbe3ab` (4/18) で AnchorMatcher 実装、`2c1f4ae` (4/19) で Gemini 導入、`4df7c8c` (4/20) で 6 段階に拡張。auto_pilot から呼ばれたことは一度もない。混同しやすいのは BG worker の Gemini OCR 補正 (画面 1 枚に対して整形を依頼) で、これは P4-P6 (画面ペアの同一性判定) と別物。

### Q. 「マスターが変わるとキャッシュは使われない？」

A. キャッシュキーは `(session_fp, master_fp, model)`。新ノード追加 → 自動的に新ペアでキャッシュミス → API 再リクエスト。一方、master OCR を手動編集しても `master_fp` は不変なのでキャッシュヒットしてしまう不整合があった (= `59d95c1` で修正)。

### Q. 「マージ時に Gemini プロセスが動くのなぜ？バックグラウンド未完了時にやるべきでは？」

A. P4-P6 はマスター現状との比較なのでマスター状態確定後 (= マージ時) 以外では動けない。BG 未完了の Gemini は OCR 補正 (per-screen) で別物。prefetch 設計は将来検討。

## 環境

- Python 3.11.8 + OpenSSL 3.6.2
- PaddleOCR 3.5.0 / google-genai 1.73.1 (urllib REST API 直接呼び出し)
- LC_TEXT_SEPARATION=on (本番)
- GEMINI_API_KEY 設定済み (P4-P6 動作中、キャッシュ累計 1,667 ペア)

## テスト結果

- anchor_matcher: 28 件 PASS
- screen_recorder: 46 件 PASS
- gemini_ocr: 16 件 PASS
- cluster_decision: 7 件 PASS
- merge_sort: 7 件 PASS
- **合計 102 件 PASS** (test_batch_processor は事前バグで 10 件 fail、main HEAD でも同数失敗)

## コミット一覧 (12 件、時系列)

| # | コミット | カテゴリ |
|---|---|---|
| 1 | `de7f282` | fix: process_session_bg 二重起動・無限リトライ解消 |
| 2 | `20cf431` | fix: sentinel 短縮 + 進捗キー不一致 |
| 3 | `5bdb478` | feat: paused 状態を導入 |
| 4 | `e12febe` | fix: 削除済みセッションを Live から除外 |
| 5 | `518071a` | fix: seed プレビューで 0 件誤表示 |
| 6 | `42b93ea` | fix: Live 不採用画像が merge に混入 |
| 7 | `59d95c1` | fix: master OCR 編集時にキャッシュ削除 |
| 8 | `62b4753` | feat: 新規追加ノードに不採用操作 |
| 9 | `5f8c864` | feat: shift+クリック → 一括不採用 |
| 10 | `7aa52a2` | fix: 空ページ問題 |
| 11 | `5683c94` | docs: 4/24-26 欠落要約 |
| 12 | `6fcf19c` | chore: OCR 学習パターン共有 |

## 次セッションへの引き継ぎ

スクリーン記録機能の実装は **ひと段落**。次の機能に着手予定。

未解決の観察項目 (STATUS.md より、急ぎなし):
- ほむら問題 (Gemini within-session 設計検討)
- phash_low_jaccard 発火頻度の観察
- 段階 3 収束 pass 数の監視
- Gemini 503 / scrcpy 黒画面 / DB locked の散発エラー

---

# 2026-04-30 後半 - マージ時系列バグ修正 (fingerprint 再設計)

## 概要

ユーザー報告「マージ後の master でダウンロードゲージが巻き戻っている」の根本対策。3 つの絡み合う原因をチェーンで特定し、Phase 0/1/2 で段階的に修正。1 コミット (`322b2b0`)、テスト 109 件 PASS。

詳細計画: `docs/fingerprint_redesign_plan.md`

## 観測された不具合

```
sort=92  fp=9cedc717a3b44331        | download 3202.68 MB
sort=93  fp=ph_cc2332d98c6624_10620 | Download 1575.86 MB ← 巻き戻り!
```

## 根本原因 (3 段チェーン)

### ❶ fingerprint 生成で数字を全削除
`screen_recorder.py:_normalize_ocr` が `re.sub(r"\d+", "")` で OCR トークン内の数字を削除。OCR が「Download 1083 MB」を 1 トークンで返すケースで:

```
"Download 1083.64MB / 3665.99 MB" → "Download .MB / . MB" → fp X
"Download 2046.34MB / 3665.99 MB" → "Download .MB / . MB" → fp X (同じ!)
```

実 DB で 25 件のバグ衝突を確認 (進捗違い、Lv.違い、Ver. 違い等)。

### ❷ merge_to_master が直接 fp 一致をチェックしない
session_fp が既存 master_fp と一致しても、AnchorMatcher が見逃せば 'new' 分類される。

### ❸ SafeInsert が seed master を削除
'new' 分類された master_fp を SafeInsert が「配置不能」と判定 → `result.skipped` → master から DELETE。30 件の seed nodes が消失していた (実 DB 確認: 削除された行に 19ca870ff371d6d4 = "Magia Exedra logo" 等を含む)。

## 修正

### ❶ fingerprint 数字保持 (`screen_recorder.py:_normalize_ocr`)

```python
# 削除した行:
#   text = re.sub(r"\d+", "", text).strip()
#   if not text: continue
```

挙動変化:
- Download/Lv./Ver./STEP/Anniversary 等の数字混じりトークンが**別 fp** に → 進捗・バージョン違いを正しく区別
- 純数字トークン (HP・タイマー・ダメージ値) は引き続き `_PURE_NUMBER_RE` で除外 → 自然な変動は同 fp 維持
- 同 dialog (数字なし) は同 fp 維持 → 5 セッション横断で同じセリフは集約

### ❷ direct_fp_match path (`cross_session_merger.py`)

`merge_to_master` の else 分岐 + `_add_all_as_new` の両方に直接 fp 一致チェックを追加:

```python
existing = SELECT 1 FROM lc_master_nodes WHERE master_fp = ? AND version_id = ?
if existing:
    UPDATE master visit_count + 1
    INSERT mapping method='direct_fp_match'
    continue  # SafeInsert に渡らないので削除されない
else:
    # 従来の 'new' 挿入経路
```

### ❸ PHASE_DEFS に direct_fp_match (`anchor_matcher.py`)

UI 表示用に order=0、label='FP'、緑色で追加。

## テスト

### 新規 (6 件)
- `test_battle_pure_number_tokens_excluded`: 純数字 (HP/ダメージ) は引き続き同 fp
- `test_battle_mixed_token_with_number_different_fingerprint`: 'Turn 3'/'Turn 5' は別 fp (新挙動)
- `test_download_progress_different_fingerprint`: 進捗違い → 別 fp (本丸)
- `test_version_string_different_fingerprint`: Ver. 違い → 別 fp
- `test_same_dialog_same_fingerprint`: 同 dialog → 同 fp 維持
- `test_merge_to_master_direct_fp_match`: 同 fp 衝突で master 削除されない

### 更新 (1 件)
- 旧 `test_battle_number_variation_same_fingerprint` を 2 つに分割 (純数字版 + 複合トークン版)

## 検証結果

### Phase 0 (事前検証)
新ロジックで:
- ✅ Download 1083/2046/3202 MB → 3 種類の別 fp
- ✅ 同 dialog → 同 fp `0d7ad1ec` (維持)
- ⚠ Turn 3/5 → 別 fp (実 DB では既に別 cluster なので問題なし)
- ✅ Lv.5/Lv.10、Ver.3.4/3.5、STEP 1/2 → 別 fp

### Phase 1 (実装)
全 109 件 PASS (anchor_matcher 29 + screen_recorder 50 + gemini_ocr 16 + cluster_decision 7 + merge_sort 7)。test_unmerge / test_batch_processor の 11 fail は事前 fixture バグ (HEAD~1 でも同数失敗)。

### Phase 2 (シミュレーション)
合成 3 セッション (各 home → download → home) で同 fp 衝突を再現:

| 段階 | master ノード数 | 結果 |
|---|---|---|
| seed (s1) | 2 | ✓ |
| s2 マージ | 2 | ✓ 削除されてない |
| s3 マージ | 2 | ✓ 削除されてない |

最終マッピング: `direct_fp_match=6, seed=3`、master visit_count: home=5, dl=3。

## 既存データの扱い

ユーザー方針「既存データの修復はなくてよい」に従い:
- 既存 lc_screens (16K)、lc_master_nodes (1,034)、lc_node_mappings (5,695)、lc_anchor_judgments (2,614) はそのまま
- 削除された 30 seed nodes は復活させない
- 新規セッションから新ロジック適用 → 自然に正しい挙動になる
- 既存 master と新セッションは AnchorMatcher のテキスト比較 (こちらは数字除去あり) で互換性を保つ

## ユーザーとの主要対話 (このフェーズ)

### Q. 「アンカー位置が間違っているかマージ処理が間違っているか」
A. **マージ処理側**。AnchorMatcher は正しく動いている範囲では問題なし。`merge_to_master` の else 分岐で同 fp 既存チェックがなく 'new' 化、SafeInsert がそれを削除していた。さらに上流の fingerprint 設計欠陥 (数字除去) で同テンプレ別進捗が衝突する構造。

### Q. 「既存データの修復はなくてよい、今後のデータが正常なら OK」
A. → マイグレーション省略、Big Bang 再構築なし。Phase 1 のみ実施。

### Q. 「Phase 0 で Turn 系がクラスタリングで吸収されない場合は？」
A. 実 DB 確認: 各 Turn 画面は既に異なる phash で別 cluster 化されている。新ロジックでも cluster 数は不変、master 数が cluster 数分だけ増える程度。

## コミット (1 件)

| コミット | 内容 |
|---|---|
| `322b2b0` | fix: マスター時系列の巻き戻り — fingerprint 数字保持 + merge 直接 fp 一致 |

## 設計書

`docs/fingerprint_redesign_plan.md` を新規作成 (Phase 0〜4 の段階計画)。Phase 1〜2 完了済み、Phase 3 (本ドキュメント) 完了で計画全完。
